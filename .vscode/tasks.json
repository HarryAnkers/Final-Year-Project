{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        },
        {
            "type": "julia",
            "command": "juliasysimagebuild",
            "problemMatcher": [
                ""
            ],
            "group": "build"
        }
    ],
    "version": "2.0.0"
}